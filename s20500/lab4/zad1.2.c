#include<stdio.h>
 
int main() {
   int a, b;
   
   printf("Program liczacy sume 2 liczb, wprowadz 2 liczby\n");
   scanf("%d %d", &a, &b);
 
   
 
   return(a+b);
   
   // aby wyświetlić wynik programu(return) w lini komend należy wpisać 
   // ./zad1b; echo $?
}